<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Perfil do Estabelecimento - Vida no Prato</title>
    <link rel="stylesheet" href="/css/vida-no-prato.css">
    <style>
        a {
            text-decoration: none !important;
        }
        a:visited {
            color: inherit;
        }
        .profile-container {
            max-width: 900px;
            margin: 40px auto;
            padding: 0 20px;
        }
        .profile-header {
            background: linear-gradient(135deg, #4caf50, #2e7d32);
            color: white;
            padding: 40px;
            border-radius: 16px;
            margin-bottom: 30px;
            text-align: center;
        }
        .profile-header h1 {
            font-size: 2.2em;
            margin-bottom: 8px;
        }
        .profile-header p {
            font-size: 1.1em;
            opacity: 0.9;
        }
        .profile-section {
            background: white;
            padding: 30px;
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
            margin-bottom: 20px;
        }
        .profile-section h2 {
            color: #2e7d32;
            font-size: 1.5em;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 2px solid #e8f5e9;
        }
        .profile-info {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
        }
        .info-item {
            padding: 15px;
            background: #f8fdf8;
            border-radius: 12px;
            border-left: 4px solid #4caf50;
        }
        .info-label {
            font-weight: 600;
            color: #2e7d32;
            font-size: 0.9em;
            margin-bottom: 4px;
        }
        .info-value {
            color: #333;
            font-size: 1.1em;
        }
        .rating-display {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .stars {
            font-size: 1.5em;
            white-space: nowrap;
            display: inline-block;
        }
        .rating-number {
            font-size: 1.1em;
            font-weight: 600;
            color: #2e7d32;
        }
        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: 20px;
        }
        .product-item {
            background: #f8fdf8;
            padding: 15px;
            border-radius: 12px;
            border: 1px solid #e8f5e9;
            text-align: center;
            transition: all 0.3s ease;
        }
        .product-item:hover {
            box-shadow: 0 4px 15px rgba(76, 175, 80, 0.2);
            transform: translateY(-4px);
        }
        .product-emoji {
            font-size: 2.5em;
            margin-bottom: 10px;
        }
        .product-name {
            font-weight: 600;
            color: #2e7d32;
            margin-bottom: 5px;
        }
        .product-price {
            color: #4caf50;
            font-size: 1.1em;
            font-weight: 600;
        }
        .empty-state {
            text-align: center;
            padding: 40px;
            color: #999;
        }
        .empty-state-icon {
            font-size: 3em;
            margin-bottom: 15px;
        }
        .description-box {
            background: #f8fdf8;
            padding: 20px;
            border-radius: 12px;
            border-left: 4px solid #4caf50;
            margin: 20px 0;
            line-height: 1.6;
        }
        .profile-container .btn-voltar-inicio {
            display: inline-flex;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <header class="header">
        <div class="container">
            <div class="header-content">
                <a href="/" class="logo">Vida no Prato</a>
            </div>
        </div>
    </header>

    <main class="main-content">
        <div class="profile-container">
            <a href="/admin" class="btn-voltar-inicio">← Voltar ao Painel</a>

        <div class="profile-header">
            <h1>🌱 <%= estabelecimento.nome %></h1>
            <p>Perfil do Estabelecimento</p>
        </div>

        <div class="profile-section">
            <h2>📋 Dados do Estabelecimento</h2>
            <div class="profile-info">
                <div class="info-item">
                    <div class="info-label">CNPJ</div>
                    <div class="info-value"><%= estabelecimento.cnpj || 'Não informado' %></div>
                </div>
                <div class="info-item">
                    <div class="info-label">E-mail</div>
                    <div class="info-value"><%= estabelecimento.email %></div>
                </div>
                <div class="info-item">
                    <div class="info-label">Telefone</div>
                    <div class="info-value"><%= estabelecimento.telefone || 'Não informado' %></div>
                </div>
                <div class="info-item">
                    <div class="info-label">Avaliação</div>
                    <div class="rating-display">
                        <span class="stars">
                            <% const notaNum = Number(estabelecimento.nota) || 5; %>
                            <% for (let i = 0; i < 5; i++) { %>
                                <% if (i < Math.floor(notaNum)) { %>⭐<% } else { %>☆<% } %>
                            <% } %>
                        </span>
                        <span class="rating-number"><%= notaNum.toFixed(1) %></span>
                    </div>
                </div>
            </div>

            <% if (estabelecimento.descricao_estabelecimento) { %>
                <div class="description-box">
                    <strong>Sobre o Estabelecimento:</strong><br>
                    <%= estabelecimento.descricao_estabelecimento %>
                </div>
            <% } %>
        </div>

        <div class="profile-section">
            <h2>📦 Produtos Cadastrados (<%= produtos.length %>)</h2>
            <% if (produtos.length > 0) { %>
                <div class="products-grid">
                    <% produtos.forEach(produto => { %>
                        <div class="product-item">
                            <div class="product-emoji"><%= produto.emoji || '🍲' %></div>
                            <div class="product-name"><%= produto.nome %></div>
                            <div style="font-size: 0.9em; color: #666; margin-bottom: 8px;">
                                <%= produto.categoria %>
                            </div>
                            <div class="product-price">
                                R$ <%= Number(produto.preco).toFixed(2).replace('.', ',') %>
                            </div>
                        </div>
                    <% }) %>
                </div>
            <% } else { %>
                <div class="empty-state">
                    <div class="empty-state-icon">📭</div>
                    <p>Nenhum produto cadastrado ainda.</p>
                    <p><a href="/admin" style="color: #4caf50; font-weight: 600;">Adicionar produtos agora</a></p>
                </div>
            <% } %>
        </div>
        </div>
    </main>
</body>
</html>
