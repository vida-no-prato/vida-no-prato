<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Perfil do Estabelecimento - Vida no Prato</title>
    <link rel="stylesheet" href="/css/vida-no-prato.css">
    <style>
        a { text-decoration: none !important; }
        a:visited { color: inherit; }
        .profile-container { max-width: 900px; margin: 40px auto; padding: 0 20px; }
        .profile-header { background: linear-gradient(135deg, #4caf50, #2e7d32); color: white; padding: 40px; border-radius: 16px; margin-bottom: 30px; text-align: center; }
        .profile-header h1 { font-size: 2.2em; margin-bottom: 8px; }
        .profile-header p { font-size: 1.1em; opacity: 0.9; }
        .profile-section { background: white; padding: 30px; border-radius: 16px; box-shadow: 0 4px 20px rgba(0,0,0,0.08); margin-bottom: 20px; }
        .profile-section h2 { color: #2e7d32; font-size: 1.5em; margin-bottom: 20px; padding-bottom: 10px; border-bottom: 2px solid #e8f5e9; }
        .profile-info { display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 20px; }
        .info-item { padding: 15px; background: #f8fdf8; border-radius: 12px; border-left: 4px solid #4caf50; }
        .info-label { font-weight: 600; color: #2e7d32; font-size: 0.9em; margin-bottom: 4px; }
        .info-value { color: #333; font-size: 1.1em; }
        .rating-display { display: flex; align-items: center; gap: 10px; }
        .stars { font-size: 1.5em; white-space: nowrap; display: inline-block; }
        .rating-number { font-size: 1.1em; font-weight: 600; color: #2e7d32; }
        .products-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); gap: 20px; }
        .product-item { background: #f8fdf8; padding: 15px; border-radius: 12px; border: 1px solid #e8f5e9; text-align: center; transition: all 0.3s ease; }
        .product-item:hover { box-shadow: 0 4px 15px rgba(76, 175, 80, 0.2); transform: translateY(-4px); }
        .product-emoji { font-size: 2.5em; margin-bottom: 10px; }
        .product-name { font-weight: 600; color: #2e7d32; margin-bottom: 5px; }
        .product-price { color: #4caf50; font-size: 1.1em; font-weight: 600; }
        .empty-state { text-align: center; padding: 40px; color: #999; }
        .empty-state-icon { font-size: 3em; margin-bottom: 15px; }
        .description-box { background: #f8fdf8; padding: 20px; border-radius: 12px; border-left: 4px solid #4caf50; margin: 20px 0; line-height: 1.6; }
        .profile-container .btn-voltar-inicio { display: inline-flex; margin-bottom: 20px; transition: transform 0.2s; cursor: pointer; }
        .profile-container .btn-voltar-inicio:hover { transform: translateX(-5px); }
    </style>
</head>
<body>

    <button class="cart-btn admin-btn-fixo" onclick="window.location.href='/admin'">Para Colaboradores</button>
    
    <header class="header">
        <div class="container">
            <nav class="nav">
                <div class="logo">Vida no Prato</div>
                
                <button class="cart-btn admin-btn-mobile" onclick="openCart(); closeMobileMenu()">
                    🛒 Carrinho
                    <span class="cart-count" id="mobileCartCount">0</span>
                </button>
                <ul class="nav-links">
 
                    <li><a href="/">Início</a></li>
                    
                    <li class="nav-item-dropdown">
                        <a href="/categories">Categorias</a> <ul class="dropdown-menu">
                            <% if (typeof categorias !== 'undefined' && categorias.length > 0) { %>
                                <% categorias.slice(0, 5).forEach(categoria => { // Mostra apenas as 5 primeiras, por exemplo
                                    if (categoria && categoria.slug) { %>
                                        <li>
                                            <a href="/categories?categoria=<%= categoria.slug %>">
                                                <%= categoria.emoji || '🥗' %> <%= categoria.nome %>
                                            </a>
                                        </li>
                                    <% } 
                                }) %>
                                <li class="dropdown-divider"></li>
                                <li><a href="/categories" class="view-all">Ver Todas as Categorias</a></li>
                            <% } else { %>
                                <li><a href="/categories">Ver Categorias</a></li>
                            <% } %>
                        </ul>
                    </li>
                    <li><a href="/#produtos">Cardápio</a></li>
                    <li><a href="/about">Sobre</a></li>
                </ul>
                <div style="display: flex; gap: 15px; align-items: center;">
                    <button class="login-btn" id="loginBtn" onclick="openAuthModal()">
                        👤 Entrar
                    </button>

                    <div class="user-menu" id="userMenu" style="display: none;">
                        <div class="user-avatar" onclick="toggleDropdown()" id="userAvatar">
                            U
                        </div>
                        <div class="dropdown" id="userDropdown" style="display: none;">
                            <a href="#" onclick="showProfile()">👤 Meu Perfil</a>
                            <a href="#" onclick="showOrders()">📦 Meus Pedidos</a>
                            <a href="#" onclick="logout()">🚪 Sair</a>
                        </div>
                    </div>

                    <button class="cart-btn" onclick="openCart()">
                        🛒 Carrinho
                        <span class="cart-count" id="cartCount">0</span>
                    </button>

                    <button class="cart-btn admin-btn-nav" onclick="window.location.href='/admin'">Para Colaboradores</button>
                </div>

                <button class="cart-btn admin-btn-fixo" onclick="window.location.href='/admin'">Para Colaboradores</button>

                <!-- Menu Hamburger -->
                <button class="hamburger-btn" id="hamburgerBtn" onclick="toggleMobileMenu()">
                    <span></span>
                    <span></span>
                    <span></span>
                </button>
            </nav>

            <!-- Mobile Menu -->
            <div class="mobile-menu" id="mobileMenu">
                <!-- Top section: Login/Profile -->
                <div class="mobile-menu-top">
                    <div id="mobileLoginSection">
                        <button class="mobile-login-btn" onclick="closeMenuAndOpenAuth()">
                            👤 Entrar / Cadastro
                        </button>
                    </div>
                    <div id="mobileUserSection" style="display: none;">
                        <div class="mobile-user-profile">
                            <div class="mobile-avatar" id="mobileUserAvatar">U</div>
                            <div class="mobile-user-info">
                                <a href="#" onclick="showProfile(); closeMobileMenu()">👤 Meu Perfil</a>
                                <a href="#" onclick="showOrders(); closeMobileMenu()">📦 Meus Pedidos</a>
                                <a href="#" onclick="logout(); closeMobileMenu()">🚪 Sair</a>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Navigation Links -->
                <ul class="mobile-nav-links">
                    <li><a href="/" onclick="closeMobileMenu()">🏠 Início</a></li>
                    <li><a href="/categories" onclick="closeMobileMenu()">🏷️ Categorias</a></li>
                    <li><a href="/#produtos" onclick="closeMobileMenu()">🍲 Cardápio</a></li>
                    <li><a href="/about" onclick="closeMobileMenu()">ℹ️ Sobre</a></li>
                </ul>

                <!-- Cart Button -->
                <div class="mobile-menu-bottom">
                    <button class="mobile-cart-btn" onclick="window.location.href='/admin'; closeMobileMenu()">
                        👥 Para Colaboradores
                    </button>
                </div>
            </div>
        </div>
    </header>
 
    <main class="main-content">
        <div class="profile-container">
            
            <div class="btn-voltar-inicio" onclick="window.location.href = '/'" style="color: #4caf50; font-weight: 600; display: flex; align-items: center; gap: 5px;">
                <span style="font-size: 1.4em;">✕</span> Voltar ao Início
            </div>
 
            <div class="profile-header">
                <h1>🌱 <%= estabelecimento.nome %></h1>
                <p>Perfil do Estabelecimento</p>
            </div>
 
            <div class="profile-section">
                <h2>📋 Dados do Estabelecimento</h2>
                <div class="profile-info">
                    <div class="info-item">
                        <div class="info-label">CNPJ</div>
                        <div class="info-value"><%= estabelecimento.cnpj || 'Não informado' %></div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">E-mail</div>
                        <div class="info-value"><%= estabelecimento.email %></div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Telefone</div>
                        <div class="info-value"><%= estabelecimento.telefone || 'Não informado' %></div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Avaliação</div>
                        <div class="rating-display">
                            <span class="stars">
                                <% const notaNum = Number(estabelecimento.nota) || 5; %>
                                <% for (let i = 0; i < 5; i++) { %>
                                    <% if (i < Math.floor(notaNum)) { %>⭐<% } else { %>☆<% } %>
                                <% } %>
                            </span>
                            <span class="rating-number"><%= notaNum.toFixed(1) %></span>
                        </div>
                    </div>
                </div>
 
                <% if (estabelecimento.descricao_estabelecimento) { %>
                    <div class="description-box">
                        <strong>Sobre o Estabelecimento:</strong><br>
                        <%= estabelecimento.descricao_estabelecimento %>
                    </div>
                <% } %>
            </div>
 
            <div class="profile-section">
                <h2>📦 Produtos Cadastrados (<%= produtos.length %>)</h2>
                
                <% if (produtos.length > 0) { %>
                    <div class="products-grid">
                        <% produtos.forEach(produto => { %>
                            <div class="product-item">
                                <div class="product-emoji"><%= produto.emoji || '🍲' %></div>
                                <div class="product-name"><%= produto.nome %></div>
                                <div style="font-size: 0.9em; color: #666; margin-bottom: 8px;">
                                    <%= produto.categoria %>
                                </div>
                                <div class="product-price">
                                    R$ <%= Number(produto.preco).toFixed(2).replace('.', ',') %>
                                </div>
                            </div>
                        <% }) %>
                    </div>
                <% } else { %>
                    <div class="empty-state">
                        <div class="empty-state-icon">📭</div>
                        <p>Nenhum produto cadastrado ainda.</p>
                        <p><a href="/admin" style="color: #4caf50; font-weight: 600;">Adicionar produtos agora</a></p>
                    </div>
                <% } %>
            </div>
        </div>
    </main>

    <%- include('partials/modals') %>

    <script src="/js/script.js"></script>
</body>
</html>